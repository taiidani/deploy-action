version: 2
updates:
  - package-ecosystem: docker-compose
    directories:
      - /alloy
      - /caddy
      - /cadvisor
      - /gitea
      - /groceries
      - /guess-my-word
      - /homepage
      - /immich
      - /jellyfin
      - /lil-dumpster
      - /monitoring
      - /no-time-to-explain
      - /plex
      - /redis
      - /servarr
      - /tfc-agent
    schedule:
      interval: "monthly"
